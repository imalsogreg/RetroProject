function new_place_cells = find_neighbors(place_cells, maxDistance)
% Attach a .neighbors field to each place cell in place_cells
% Place cell B is a neighbor of A if field B is close to A, and the
% directionality of A 'points toward' B.

nCells = numel(place_cells.field_centers);

for c = 1:nCells


end

end